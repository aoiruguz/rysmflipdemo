# 场景跳转逻辑更新 - 完成总结

## 更新内容

### 1. **新的场景跳转流程**

#### 正常完成歌曲
1. 玩家击打最后一个note
2. `NoteManager.OnNoteProcessed()` 检测到所有note已处理
3. 显示结算文本：
   - **All Perfect!!** - 所有判定都是Perfect
   - **Full Combo!** - 无Miss，所有note都击中
   - **Song Clear!** - 正常完成
4. 等待3秒
5. 跳转到Game Over Scene（结算界面）

#### 游戏失败（血量耗尽）
1. `HealthSystem.OnGameOver()` 被触发
2. 显示 **Game Over** 文本
3. 等待2秒
4. 跳转到Game Over Scene（结算界面）

### 2. **Note处理追踪系统**

#### NoteManager.cs 更新
- ✅ 添加了 `totalNotes` - 谱面总note数
- ✅ 添加了 `processedNotes` - 已处理的note数（击打或miss）
- ✅ 添加了 `songCompleted` - 标记歌曲是否已完成
- ✅ 新增 `OnNoteProcessed()` 方法 - 追踪note处理状态
- ✅ 新增 `OnSongCompleted()` 协程 - 处理歌曲完成逻辑
- ✅ 更新 `SpawnNoteFromData()` - miss回调中调用 `OnNoteProcessed()`
- ✅ 移除了 `PlayRoutine()` 中的自动跳转逻辑

#### PlayerController.cs 更新
- ✅ 在note被击打后调用 `NoteManager.OnNoteProcessed()`

### 3. **UI显示更新**

#### UIManager.cs 更新
- ✅ 添加了 `songClearText` 字段 - 用于显示结算文本
- ✅ 新增 `ShowSongClearText()` 方法 - 显示Song Clear/Full Combo/All Perfect
- ✅ 根据文本类型设置不同颜色：
  - All Perfect → 粉紫色 (1, 0.5, 1)
  - Full Combo → 黄色
  - Song Clear → 白色
  - Game Over → 白色

### 4. **Retry功能**

#### ResultScreenNavigation.cs 更新
- ✅ Retry按钮保持 `SongSelectionManager.selectedChart` 不变
- ✅ PlayScene会自动读取之前选择的谱面
- ✅ Back按钮清理PlayDataCollector并返回选曲界面

#### PlayDataCollector.cs 更新
- ✅ 在 `Start()` 中重置 `ScoreManager` 统计数据
- ✅ 确保每次进入PlayScene都是全新的游玩数据

#### HealthSystem.cs 更新
- ✅ Game Over时显示文本并等待2秒后跳转
- ✅ 添加了 `LoadGameOverSceneAfterDelay()` 协程

## 工作流程

### 场景1：正常完成歌曲
```
PlayScene
  ↓ 玩家击打最后一个note
NoteManager.OnNoteProcessed()
  ↓ processedNotes == totalNotes
NoteManager.OnSongCompleted()
  ↓ 收集数据
PlayDataCollector.CollectFinalData()
  ↓ 判断成就
IsAllPerfect() / IsFullCombo() / 普通Clear
  ↓ 显示文本
UIManager.ShowSongClearText("All Perfect!!")
  ↓ 等待3秒
WaitForSeconds(3f)
  ↓ 跳转
LoadScene("Game Over")
  ↓
ResultScreenUI 显示结算数据
```

### 场景2：游戏失败
```
PlayScene
  ↓ 血量耗尽
HealthSystem.OnGameOver()
  ↓ 收集数据
PlayDataCollector.CollectFinalData()
  ↓ 显示文本
UIManager.ShowSongClearText("Game Over")
  ↓ 等待2秒
WaitForSeconds(2f)
  ↓ 跳转
LoadScene("Game Over")
  ↓
ResultScreenUI 显示结算数据
```

### 场景3：Retry重试
```
Game Over Scene
  ↓ 点击Retry按钮
ResultScreenNavigation.OnRetryClicked()
  ↓ 保持selectedChart不变
LoadScene("PlayScene")
  ↓
NoteManager.Start()
  ↓ 读取谱面
SongSelectionManager.GetSelectedChart()
  ↓ 重置统计
PlayDataCollector.Start() → ScoreManager.ResetStats()
  ↓
开始新的游玩
```

### 场景4：返回菜单
```
Game Over Scene
  ↓ 点击Back按钮
ResultScreenNavigation.OnBackToMenuClicked()
  ↓ 清理数据
Destroy(PlayDataCollector.Instance)
  ↓ 跳转
LoadScene("SongSelection")
  ↓
重新选择歌曲
```

## 需要在Unity中设置的UI元素

### PlayScene的UIManager
需要添加一个新的Text组件用于显示结算文本：

1. 在PlayScene的Canvas中创建一个Text（或TextMeshProUGUI）
2. 命名为 "SongClearText"
3. 设置属性：
   - Font Size: 60-80
   - Alignment: Center
   - Position: 屏幕中央
   - 初始状态：Inactive（脚本会自动激活）
4. 将这个Text拖到UIManager组件的 `songClearText` 字段

### 推荐的UI布局
```
Canvas
├── ComboText
├── JudgmentText
├── OffsetText
├── SongTitleText
├── TimeDisplayText
└── SongClearText (新增) ← 屏幕中央，大字体
```

## 测试步骤

### 测试1：All Perfect
1. 进入PlayScene
2. 完美击打所有note（全部Perfect）
3. 最后一个note后应显示 "All Perfect!!"（粉紫色）
4. 3秒后跳转到结算界面
5. 结算界面应显示All Perfect图标

### 测试2：Full Combo
1. 进入PlayScene
2. 击打所有note（允许Great/Good，但不能Miss）
3. 最后一个note后应显示 "Full Combo!"（黄色）
4. 3秒后跳转到结算界面
5. 结算界面应显示Full Combo图标

### 测试3：Song Clear
1. 进入PlayScene
2. 正常游玩（有Miss）
3. 最后一个note后应显示 "Song Clear!"（白色）
4. 3秒后跳转到结算界面

### 测试4：Game Over
1. 进入PlayScene
2. 故意Miss很多note让血量耗尽
3. 应显示 "Game Over"（白色）
4. 2秒后跳转到结算界面

### 测试5：Retry功能
1. 完成一首歌曲到结算界面
2. 点击Retry按钮（或按R键）
3. 应重新加载PlayScene并播放相同的谱面
4. 统计数据应重置为0

### 测试6：Back功能
1. 在结算界面点击Back按钮（或按ESC键）
2. 应返回到SongSelection场景
3. 可以重新选择其他歌曲

## 注意事项

1. **UIManager的songClearText必须设置**
   - 如果未设置，会在Console中看到警告，但不会崩溃
   - 文本不会显示，但跳转仍会正常工作

2. **PlayDataCollector使用DontDestroyOnLoad**
   - 从PlayScene到Game Over Scene数据会保留
   - Retry时会重置统计数据
   - Back时会销毁PlayDataCollector

3. **SongSelectionManager.selectedChart是静态的**
   - Retry功能依赖这个静态变量
   - 只有在Back返回选曲界面后才会改变

4. **note处理计数很重要**
   - 每个note必须被处理（击打或miss）
   - 如果有note卡住不动，歌曲不会结束
   - 确保所有note都有正确的miss回调

## 更新的文件列表

1. `Assets/Scripts/NoteManager.cs` - note追踪和歌曲完成逻辑
2. `Assets/Scripts/PlayerController.cs` - 击打时通知NoteManager
3. `Assets/Scripts/UIManager.cs` - 显示结算文本
4. `Assets/Scripts/ResultScreenNavigation.cs` - Retry逻辑优化
5. `Assets/Scripts/PlayDataCollector.cs` - 重置统计数据
6. `Assets/Scripts/HealthSystem.cs` - Game Over显示和延迟跳转

## 完成！

新的场景跳转系统已完全实现：
- ✅ 最后一个note完成后显示结算文本
- ✅ 根据成就显示不同文本（All Perfect / Full Combo / Song Clear）
- ✅ 等待3秒后跳转到结算界面
- ✅ Retry按钮正确重新加载相同谱面
- ✅ Back按钮返回选曲界面
- ✅ Game Over也有相应的显示和跳转

现在可以在Unity中测试完整的游戏流程了！
